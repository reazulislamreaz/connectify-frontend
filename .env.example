# REST API via Next.js proxy (recommended — avoids CORS)
NEXT_PUBLIC_API_URL=/api

# Local dev (chatting-app-backend on PORT=8081)
BACKEND_PROXY_URL=http://localhost:8081
NEXT_PUBLIC_SOCKET_URL=http://localhost:8081
NEXT_PUBLIC_UPLOADS_URL=http://localhost:8081

# VPS (HTTP — works for http://localhost:3000 only)
# BACKEND_PROXY_URL=http://3.150.217.155/connectify
# NEXT_PUBLIC_SOCKET_URL=http://3.150.217.155
# NEXT_PUBLIC_SOCKET_PATH=/connectify/socket.io
# NEXT_PUBLIC_UPLOADS_URL=http://3.150.217.155/connectify

# Vercel / HTTPS frontend (https://easy-connectify.vercel.app)
# Backend MUST be reachable over HTTPS/WSS (use Cloudflare proxy on a domain).
# BACKEND_PROXY_URL=https://api.yourdomain.com/connectify
# NEXT_PUBLIC_SOCKET_URL=http://3.150.217.155
# NEXT_PUBLIC_SOCKET_SECURE_URL=https://api.yourdomain.com
# NEXT_PUBLIC_SOCKET_PATH=/connectify/socket.io
# NEXT_PUBLIC_UPLOADS_URL=https://api.yourdomain.com/connectify
#
# Backend CLIENT_URL must include: https://easy-connectify.vercel.app

# ZEGOCLOUD credentials live on the backend only (see chatting-app-backend/.env.example)
